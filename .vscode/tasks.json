{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build bundle",
            "group": "build",
            "command": "mvn",
            "args": [
                // "-o",
                "clean",
                "verify"
            ],
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    "JAVA_HOME": "${userHome}/.sdkman/candidates/java/21.0.2-open"
                }
            }
        }
    ]
}